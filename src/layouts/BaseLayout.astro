---
import "@/styles/base.css";
import "@fontsource-variable/raleway";

import Meta from "@/components/base/Meta.astro";
import type { MetaTags as Props } from "@/types/Meta";

const meta = Astro.props;
---

<!DOCTYPE html>
<html lang="pt" class="scroll-smooth">
  <head>
    <Meta {...meta} />
  </head>

  <!-- lining-nums fixes raleway font oddly placed numbers -->
  <body class="bg-primary h-full w-full font-primary lining-nums">
    <slot />
  </body>

</html>
<script>
import { setPreferredTheme, isLightMode, getThemeStr } from "@/resources/stores/theme-store";

setPreferredTheme()
document.addEventListener("astro:after-swap", () => document.firstElementChild?.setAttribute("page-theme", getThemeStr(isLightMode.get())));
</script>
